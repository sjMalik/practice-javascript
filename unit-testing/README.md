## Steps
1. npm i mocha -g
2. npm i chai
3. mocha chai-example.js

## Output
```
  Simple Calculation
This will execute once before all the tasks

This will execute before every test
    ✔ Its giving 4 whene we add 2+2
This will execute after each test

This will execute before every test
    ✔ Its giving boolean value
This will execute after each test

This will execute before every test
    ✔ Checking String length
This will execute after each test

This will execute before every test
    ✔ Checking object property
This will execute after each test

This will execute after all the tests


  4 passing (8ms)
  ```